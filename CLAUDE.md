# CLAUDE.md

## Project overview

Static HTML book generated from a Dec 2022 Twitter export. The book is titled "vgr: The Twitter Years, 2007–22" by Venkatesh Rao. It contains 102 chapters: a "Singles" compendium of 396 curated individual tweets (including 10 prehistory tweets from 2007), plus 101 thread chapters ordered chronologically. Each thread chapter has an AI-generated summary in "In which I..." first-person voice.

## Build commands

```bash
# Standard rebuild (run both, in this order):
python3 data/generate_compendium_chapter.py
python3 data/generate_book_html_with_titles.py

# Preview:
cd book && python3 -m http.server 8000

# Deploy to venkateshrao.com/twitter-book:
bash deploy.sh
cd ~/Dropbox/Code/consulting/vgururao.github.io
git add twitter-book/ && git commit -m "Update twitter-book" && git push
```

The book generator clears and regenerates all `book/chapters/chapter_*.html` files each run. It also regenerates cover, title, ToC, and preface pages. It patches navigation into the compendium page (generated separately).

## Architecture: three layers

1. **Data normalization + cleanup** — normalizing raw tweets, building threads, applying patches/ops
2. **Content generation** — `generate_compendium_chapter.py` and the thread chapter rendering in `generate_book_html_with_titles.py`
3. **Book scaffolding + navigation** — page shells, nav bars, ToC, frontmatter rendering (all in `generate_book_html_with_titles.py`)

Shared tweet text rendering lives in `data/tweet_text_cleanup.py` (used by both generators).

## Key files

| File | Role | Editable? |
|------|------|-----------|
| `data/frontmatter/*.html` | Cover, title, preface fragments | Hand-edited, never auto-overwritten |
| `data/chapter_titles.json` | Chapter title overrides | Hand-edited |
| `data/url_overrides.json` | Broken/hijacked URL replacements | Hand-edited |
| `data/selection_final_clean.json` | Selected thread IDs + compendium tweet IDs | Pipeline output |
| `data/url_titles.json` | Cached page titles for URLs | Generated by `fetch_url_titles.py` |
| `data/chapter_summaries.json` | Per-chapter summaries for ToC tooltips and chapter headers | Hand-editable |
| `data/compendium_tagged_with_prehistory.json` | Compendium tweet selection (includes 2007 prehistory) | Hand-editable |
| `book/style.css` | Main styling (Georgia serif, warm parchment palette) | Hand-edited, never auto-overwritten |
| `book/compendium.css` | Singles chapter styling (tag colors, featured tweets, stats) | Hand-edited, never auto-overwritten |
| `book/mobile.css` | Mobile-responsive overrides (≤600px) | Hand-edited, never auto-overwritten |
| `book/**/*.html` | Generated output | Regenerated each build |
| `deploy.sh` | Build + rsync to GitHub Pages site repo | Script |

## Constraints

- **Python 3.9 compatibility**: No backslashes inside f-string expressions (allowed only in Python 3.12+). Extract to variables first.
- **`book/style.css`, `book/compendium.css`, and `book/mobile.css` are sacred**: Never overwrite. Only append/edit specific rules.
- **Frontmatter fragments are sacred**: Read from `data/frontmatter/`, never auto-rewritten. Generators create defaults only if files are missing.
- **Media only for selected content**: `copy_media_to_book.py` filters to selected threads + compendium tweets only.
- **Chapter numbering**: Compendium is chapter 1; threads start at 2, ordered chronologically.
- **Reading order**: Cover → Title → ToC → Preface → Singles → Thread Chapters.

## Tweet text rendering pipeline (tweet_text_cleanup.py)

1. Unescape HTML entities (`&gt;` → `>`)
2. Strip media t.co URLs (media rendered separately as embeds)
3. Replace entity URLs with placeholders
4. Escape remaining text for HTML
5. Replace placeholders with rendered links:
   - Self-tweet URLs: stripped from text (content shown as quote box instead)
   - Other-people tweet URLs: rendered as "tweet" + superscript endnote `[N]`
   - Regular URLs: anchor text from fallback chain (url_overrides title → fetched title → display_url → "link")
6. Apply `*bold*` and `_italics_` markdown
7. Convert newlines to `<br>`

## URL systems

- **url_titles.json**: Cached page titles, fetched by `fetch_url_titles.py`. ~237 entries. Supports `--retry` for failed entries.
- **url_overrides.json**: Manual replacements for hijacked/broken URLs. Structure: `{overrides: [{original_url, replacement_url, title, reason}]}`.
- **Endnotes**: External tweet links get per-chapter endnotes at chapter bottom showing `@handle — full URL`.

## Deployment

- **Live URL**: https://venkateshrao.com/twitter-book/
- **Hosting**: GitHub Pages via `vgururao.github.io` repo
- **Site repo**: `~/Dropbox/Code/consulting/vgururao.github.io`
- **Deploy script**: `bash deploy.sh` builds book and rsyncs to `twitter-book/` in site repo
- **Versioning**: Git tags in this repo (current: `v1.0`)

## Completed (v1.0)

- Typography overhaul: Georgia serif body, system-ui sans titles, warm parchment/cream palette
- Singles chapter redesign: per-tag pastel colors, featured tweet highlighting, stats box, 2007 prehistory tweets
- Chapter summaries: 101 AI-generated summaries in `chapter_summaries.json`, shown in chapter headers and ToC tooltips
- Navigation: boxed nav bar with HTML arrow entities, "Table of Contents" label
- ToC: hover tooltips with summaries, dates in parentheses, clean flat list
- Preface: full author-written text with quoted tweet
- Deployed to venkateshrao.com/twitter-book/

## Completed (v1.1)

- Page `<title>` metadata: book title on cover/title, `Book Title | Page Name` on all other pages
- Mobile-responsive CSS (`book/mobile.css`): viewport meta, compact nav, readable text sizing for phones
- Social cards: Open Graph and Twitter Card meta tags on every page (cover image, per-chapter summaries as descriptions)
- Compendium tweet permalinks: `id` anchors + hover-reveal link emoji for direct linking to individual singles
- Title page: author name linked to venkateshrao.com, copyright statement
- Constants `BOOK_TITLE`, `SITE_BASE_URL`, `COVER_IMAGE_URL` in generator for reuse

## Completed (v1.2)

- Floating chapter navigation (Ch+/Ch−/ToC) on all pages, fixed to viewport outside content box
- Tweet permalink anchors with copy-to-clipboard + persistent highlight on all chapters (compendium + threads)
- Chapter title link-copy emoji on every page (copies page URL, no anchor)
- Hash-link auto-scroll + highlight on page load (for shared tweet permalinks)
- Fix double-escaped HTML entities (`&amp;gt;` etc.) in compendium `auto_link_text` and quote boxes
- Title page: edition note (online version, Contraptions newsletter link), "Note on Production" box (ChatGPT/Claude Code phases, GitHub repo link)
- Mobile fix: `overflow-x: hidden` + proper floating nav repositioning (fixes horizontal scroll bounce)
- Preface typo fixes

## Next session priorities

1. **ePub generation** — generate an ePub version of the book for e-readers (Kindle, Apple Books, etc.)
2. **Print/PDF generation** — generate a print-ready PDF version, with appropriate page layout, margins, and typography
3. **Ongoing content edits** — chapter summaries, preface tweaks, adding/removing singles

## Future: full archive as queryable corpus + oracle

After offline versions are done, augment the book with the full ~150k tweet archive in queryable form. Four-phase plan:

1. **Normalize full corpus** — Clean up the entire archive (not just curated 102 chapters) into a single canonical format (JSON or Parquet). Consistent fields: id, timestamp, text, thread_id, reply_to, media refs, metrics. Full thread reconstruction for all threads, not just selected ones.

2. **IPFS archival** — Pin the rendered book + full corpus on IPFS for permanent addressability. Use a pinning service (Pinata, web3.storage, or Filecoin) for persistence. CID becomes the canonical reference. Keep GitHub Pages as primary access; IPFS as the permanence/memorialization layer.

3. **RAG-based query interface** — Embed all tweets into a vector store, build a semantic search + retrieval-augmented generation interface. Allows queries like "what did vgr say about X?" with cited source tweets. Could be a lightweight web app or CLI tool.

4. **Fine-tuned oracle (experimental)** — Explore fine-tuning or persona distillation to create a model that can respond in vgr's voice/style. Hybrid approach recommended: RAG for factual grounding + system prompt for personality. Consider mixing in ribbonfarm/blog content for richer training signal. 150k tweets alone may not be enough for convincing fine-tuning.

## Future: make repo forkable as a starter template

After the book is finished (online + offline), do a cleanup pass to make this repo usable as a starting point for anyone else wanting to curate their Twitter archive into a book. Not a fully automated tool — a forkable template that someone can modify for their own needs.

Key changes needed:
- Extract Twitter handle (`vgr`) from hardcoded regexes into a config parameter
- Add a config file for book metadata (title, author, subtitle)
- Document the curation workflow (how to select threads, write chapter titles, curate compendium)
- Remove vgr-specific data files or move them to an `example/` directory
- Clean up `_old` script variants
- Possibly improve the thread selection/cleanup UI

## Known issues / pending work

- Ghost thread `1279451428302422016` in `selection_final_clean.json` (in selection but missing from thread data)
- Chapter 57 title in `chapter_titles.json` contains a raw t.co URL
- Many `_old` / `_old2` script variants in `data/` could be cleaned up
- Future: retrieve other-people's tweets via Wayback Machine for quote-box embedding
